import json
from difflib import get_close_matches
data = json.load(open('original.json'))


def matches(word):
   return

def search(word):
    word = word.lower()
    error = ["WORD NOT FOUND"]





    else:
        return error
